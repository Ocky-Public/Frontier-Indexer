services:
  db:
    image: docker.io/timescale/timescaledb-ha:pg17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
      PGDATA: /home/postgres/pgdata/data
    ports:
      - "5432:5432"
    volumes:
      - frontier-timescaledb-data:/home/postgres/pgdata

  indexer:
    image: frontier-indexer:local
    build:
      context: .
      args:
        PROFILE: release
        GIT_REVISION: ${GIT_REVISION:-local}
        BUILD_DATE: ${BUILD_DATE:-unknown}
    depends_on:
      - db
    environment:
      DB_HOST: db
      DB_PORT: "5432"
      DB_NAME: postgres
      DB_USER: postgres
      DB_PASSWORD: postgres
      DB_SCHEMA: indexer
      METRICS_ADDRESS: 0.0.0.0:9184
    env_file:
      - ${INDEXER_ENV_FILE:-.env}
    ports:
      - "9184:9184"
    entrypoint:
      - /bin/sh
      - -ec
      - |
        attempts=0
        max_attempts=60
        until pg_isready -h "$${DB_HOST}" -p "$${DB_PORT}" -U "$${DB_USER}" -d "$${DB_NAME}"; do
          attempts=$$((attempts + 1))
          if [ "$$attempts" -ge "$$max_attempts" ]; then
            echo "ERROR: Postgres did not become ready after $$max_attempts seconds (host=$${DB_HOST} port=$${DB_PORT} db=$${DB_NAME})." >&2
            exit 1
          fi
          sleep 1
        done
        exec /opt/indexer/bin/indexer

volumes:
  frontier-timescaledb-data: